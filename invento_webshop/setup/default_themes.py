# Default themes are shipped as fixtures in fixtures/webshop_theme.json.
# SVG assets live in public/images/themes/{slug}/ and are served at
# /assets/invento_webshop/images/themes/{slug}/ in development mode.
